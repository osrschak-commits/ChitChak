{
  "name": "@chitchak/desktop",
  "version": "0.1.2",
  "private": true,
  "description": "Voice-first chat for small groups",
  "author": "ChitChak",
  "type": "module",
  "main": "dist-electron/main.cjs",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "icon": "node scripts/make-icon.mjs",
    "dist:win": "node scripts/make-icon.mjs && node scripts/build.mjs && electron-builder --win --config electron-builder.yml",
    "typecheck": "tsc --noEmit"
  },
  "//": "livekit-client is pinned exactly, not with a caret. It negotiates an RTC protocol version with the SFU, and a client newer than the server in docker-compose.yml connects, silently falls back, and then reports 'reconnecting' forever while media actually flows. Upgrade this and the livekit-server image together.",
  "dependencies": {
    "@chitchak/protocol": "*",
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "electron-updater": "^6.8.9",
    "livekit-client": "2.22.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^5.0.3"
  },
  "devDependencies": {
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "33.4.11",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
